{
  "name": "junghun-chae-research-site",
  "private": true,
  "type": "module",
  "scripts": {
    "build:assets": "node scripts/prepare-assets.mjs",
    "dev": "npm run build:assets && wrangler dev",
    "deploy": "npm run build:assets && wrangler deploy",
    "check": "npm run build:assets && node --check src/index.js && node --check src/retrieval.js && node --check src/prompt.js && node --check src/providers/cloudflare.js && node --check src/providers/index.js"
  },
  "devDependencies": {
    "wrangler": "^4.36.0"
  }
}
